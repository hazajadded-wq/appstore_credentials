name: salaryinfo
description: بوابة الموظف الرقمية - الشركة العامة لتعبئة وخدمات الغاز
publish_to: "none"

version: 1.0.12+15

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ✅ EMERGENCY FIX: ONLY ESSENTIAL DEPENDENCIES
  # Removed Firebase temporarily to fix black screen

  # 🌐 WebView - ESSENTIAL
  webview_flutter: ^4.4.4
  webview_flutter_wkwebview: ^3.23.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png