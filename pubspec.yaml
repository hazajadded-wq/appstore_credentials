name: salaryinfo
description: بوابة الموظف الرقمية - الشركة العامة لتعبئة وخدمات الغاز
publish_to: "none"

version: 1.0.11+5

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase - COMPATIBLE VERSIONS
  firebase_core: ^4.3.0
  firebase_messaging: ^16.1.0

  # 🌐 WebView
  webview_flutter: ^4.4.4  # Keep this stable version
  webview_flutter_android: ^4.10.11
  webview_flutter_wkwebview: ^3.23.5

  # 🎨 UI & Design
  google_fonts: ^6.3.3
  cached_network_image: ^3.3.1

  # 💾 Storage & Permissions - USE WORKING VERSIONS
  shared_preferences: ^2.2.2  # Keep existing version
  permission_handler: ^12.0.1  # Keep existing version
  device_info_plus: ^10.1.2  # Keep existing version
  gal: ^2.3.2  # USE THIS VERSION (compatible with your code)
  path_provider: ^2.1.2  # Keep existing version

  # 🧰 Utilities
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
