name: salaryinfo
description: بوابة الموظف الرقمية - الشركة العامة لتعبئة وخدمات الغاز
publish_to: 'none'

version: 1.0.11+5

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 🔥 Firebase
  firebase_core: ^2.24.2
  firebase_messaging: ^14.7.10

  # 🌐 WebView (لا تضف android / wkwebview يدويًا)
  webview_flutter: ^4.5.2

  # 🎨 UI & Design
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.0

  # 💾 Storage & Permissions
  shared_preferences: ^2.2.2
  permission_handler: ^10.4.4
  device_info_plus: ^9.1.0
  gal: ^2.2.1
  path_provider: ^2.1.0

  # 🔗 Network & Utilities
  http: ^1.1.0
  url_launcher: ^6.2.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/logo.png

  generate: true
